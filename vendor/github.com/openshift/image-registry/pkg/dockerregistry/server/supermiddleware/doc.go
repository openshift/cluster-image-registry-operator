// Package supermiddleware provides helpers to run the Docker registry with
// middlewares without having to register them globally. Also it allows having
// a custom blob descriptor service for each repository.
package supermiddleware
